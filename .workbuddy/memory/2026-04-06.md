# 2026-04-06

## 正数计时器（秒表）功能

- `TestTimer.jsx` 重构：拆分为 `Countdown`（倒计时）+ `StopwatchPanel`（正数计时器）两个子组件
- 顶部 Tab 切换（倒计时 / 正数计时），统一在 Timer 页签下
- 秒表功能：Start/Pause + Lap（记录分段时间）+ Reset
- 圆环进度条以 60 秒为一圈循环旋转，颜色随累计时间变化（蓝→绿→黄→橙）
- Lap 列表倒序显示，最多可显示若干条（可滚动）
- 时间格式支持超过 1 小时（hh:mm:ss）

## 回滚

- 正数计时器（秒表）功能因导致白屏已回滚，`TestTimer.jsx` 恢复为纯倒计时版本

## Daily Todo 功能

- 新增 `src/hooks/useTodoList.js`：数据层 hook，localStorage 持久化
  - 每日自动刷新：检测 lastDate，跨日则重置所有 done → false
- 新增 `src/components/DailyTodo.jsx`：UI 组件
  - 增删改查 + 勾选完成 + 双击行内编辑
  - 进度条显示完成百分比
  - 全部完成时显示庆祝横幅
  - "Clear done" 一键删除已完成项
- App.jsx 新增 "Daily Tasks" 页签（ListTodo 图标）
- 任务标签：R(Reading)/L(Listening)/W(Writing)/S(Speaking)，添加/编辑时可选
- 顶部标签筛选栏：点击标签过滤列表，"All"显示全部
