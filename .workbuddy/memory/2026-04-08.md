# 2026-04-08

## Daily Tasks "Reset done" 行为修改
- `useTodoList.js` `clearDone`：从删除已完成任务(`filter`)改为将已完成任务重置为未完成（`map` 设 `done: false`）
- `DailyTodo.jsx`：按钮图标由 `Trash2` 换为 `RotateCcw`，文案由 "Clear done" 改为 "Reset done"，hover 色由红色改为 indigo

## ProgressDashboard 精简
- 删除 `Overall Progress Trend` 折线图及其 `overallSeries` 数据
- 删除 `Average Overall` 和 `Best Performance` 两个 StatCard
- 只保留 `Total Tests` 统计卡，`Row 1` 改为单卡布局

## Detailed Records 表格排序功能
- 两种排序方式：`Time`（按时间戳）和 `Book`（按书号→测试号）
- 升降序切换按钮（Asc/Desc），图标 ArrowUp/ArrowDown
- 移除 Export、Import 按钮及整条导入链路（handleImportFile、importRef、onExport/onImport prop）
- `App.jsx` 中同步移除 ProgressDashboard 的 `onExport`、`onImport` prop
- 表格排序变量：`sortMode`（"time"|"book"）、`sortDir`（"asc"|"desc"）
- 表格渲染数据从 `filteredRecords` 改为 `tableRecords`（filter + sort 后）

## Subject Score Trends 图表重构（ECharts）
- 安装 `echarts` + `echarts-for-react`，替换原有 SVG 原生图表
- X 轴改为按 book → testNum 排序，标签格式 "C{book}-T{testNum}"（如 C4-T1）
- 支持鼠标滚轮缩放（dataZoom inside）
- hover 显示具体录入日期（date）
- `ProgressLineChart` 新接口：`series: { label, color, data, dates, labels }`
- `ProgressDashboard` 中 `chartRecords` 排序由 timestamp 改为 `book → testNum`

## 听力评分换算修正
- `src/utils/scoring.js` `getListeningBand` 修正三处临界值：
  - 7.5 分：`>= 32` → `>= 33`（对应原题 33-34）
  - 6.5 分：`>= 26` → `>= 27`（对应原题 27-29）
  - 5.5 分：`>= 18` → `>= 20`（对应原题 20-22）
