# 2026-04-11

## 删除 Total Tests 栏目
- `ProgressDashboard.jsx`：移除 "Row 1: Stats" 区块（StatCard + ClipboardList 图标引用）
- 同步删除不再使用的 `StatCard` 组件定义和 `ClipboardList` 图标导入

## Import/Export 重新实现
- 新建 `src/utils/dataSync.js`：统一导出/导入三个 localStorage 数据集（records、studyProgress、dailyTodos）
- `ProgressDashboard` 工具栏新增 Export（Download）和 Import（Upload）按钮
- `useStudyProgress` 和 `useTodoList` 新增 `reload()` 方法，reload 后通过 EventBus 通知所有监听组件刷新
- `App.jsx` 中 `handleExport` / `handleImport` 连接所有逻辑，导入成功后 toast 提示数量统计
- Export/Import 按钮最终位于 **Study Log** 工具栏（Add/Delete 左侧），Detailed Records 工具栏保持整洁
- 按钮布局：`[Book selector] [Test selector] [Export] [Import] [Delete/Add]`
