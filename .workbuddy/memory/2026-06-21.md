- ProgressDashboard: Subject Score Trends 图表新增 Time/Book 排序切换按钮（与 Detailed Records 表格样式一致），新增 `chartSortMode` 状态独立控制图表排序。
- ProgressDashboard: 按Time排序时x轴不显示日期，统一用C#-T#标签（日期仅在tooltip展示）。
- ProgressLineChart: `connectNulls` 从 false 改为 true——跳过 null 数据点直接连线，不再因缺科分数导致折线断开。
