# 2026-04-07

## Bug 修复

- `DailyTodo.jsx` 暂停后 Resume 按钮消失：`pauseTimer` 将 `startedAt` 置 null，导致 `isRunning` 判假，LiveTicker 卸载
  - 修复：`isRunning = todo.startedAt != null || todo.pausedAt != null`

## Study Log 功能

- 新增 `src/hooks/useStudyProgress.js`：独立 localStorage（key: `ielts_study_progress`）
  - 按 `"book-testNum"` 键存储，L 4个section × {做完,精听,跟读}，R 3个passage × {做完,查单词,复习}
  - 暴露：`toggleStep`, `getEntry`, `allEntries`, `deleteEntry`, `initEntry`
- 新增 `src/components/StudyLog.jsx`：
  - AddEntryForm：输入 Book/Test number 创建条目
  - BookGroup → TestCard → SubjectBlock → ItemRow 树形结构
  - 进度环（SVG Ring）：每层级实时显示完成比率
  - 全部完成时有高亮反馈 + "全部完成 ✓" 标签
  - 删除二次确认 Modal
- `ProgressDashboard.jsx`：在 Detailed Records 下方追加 StudyLog 面板

## UI 语言切换

- 用户要求 UI 全面使用英文
- `StudyLog.jsx` 所有中文文案改为英文：步骤标签 (Done/Intensive/Shadowing/Vocab/Review)、按钮 (Add/Cancel/Delete)、提示文字、Modal 标题等
- 修正 "Shadowing" key 大小写 bug（保持与 hook 一致：`shadowing`）
