# 2026-06-22

## Vocab 计数器改造
- 将 Study Log 中 Reading 的 "Vocab" 标签从布尔勾选改为点击计数器（和 Review 一致）
- 修改 `useStudyProgress.js`: `makeReadingPassages` 中 vocab 初始值从 `false` 改为 `0`；`toggleStep` 中 vocab 和 review 统一走递增逻辑；`decrementReview` 重命名为 `decrementCounter`，支持 vocab/review 任意计数器字段的递减
- 修改 `StudyLog.jsx`: `ItemRow` 中 vocab 也支持右键递减；`handleDecrement` 传递 step key 给 `decrementCounter`
- 兼容旧数据：vocab 可能是 boolean（旧数据），递增/递减时做了 `typeof === "number"` 判断，boolean true 视为 1，false 视为 0
- 构建验证通过
