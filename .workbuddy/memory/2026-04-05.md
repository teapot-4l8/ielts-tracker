# 2026-04-05

## 代码结构重构 + localStorage 持久化

对 IELTS Tracker 进行了完整的代码拆分和数据持久化改造：

- 新增 `src/utils/scoring.js`：雅思分数转换逻辑
- 新增 `src/utils/geminiApi.js`：Gemini API 封装（带指数退避重试）
- 新增 `src/hooks/useRecords.js`：localStorage 持久化 hook（支持增删/导出/导入）
- 新增 `src/components/ProgressLineChart.jsx`：SVG 折线图
- 新增 `src/components/WritingGrader.jsx`：AI 作文批改面板
- 新增 `src/components/ScoreInputForm.jsx`：成绩录入表单
- 新增 `src/components/ProgressDashboard.jsx`：进度面板（含 Export/Import/Clear All）
- 重写 `src/App.jsx`：精简至 ~60 行，只做 tab 路由 + toast

数据存储在 localStorage `"ielts_tracker_records"`，支持 JSON 导出/导入备份。

## 部分保存 + 四科趋势图（第二轮）

- ScoreInputForm：至少填一项即可保存，Overall Band 仅在四科齐全时计算，否则存 null
- ProgressLineChart：重构支持多条曲线（series 模式），null 值自动跳过不断线
- ProgressDashboard：新增 "Subject Score Trends" 卡片，L/R/W/S 四条曲线同图展示；表格空值显示 —

## 记录合并/Upsert功能（第四轮）

- useRecords：新增 `upsertRecord(record)`，按 book+testNum 匹配；已有记录则合并（新非null值覆盖旧值，null不覆盖），自动重算 overallBand
- ScoreInputForm：接收 `records` prop，切换 Book/Test 时自动回填已有成绩到各输入框
- 保存按钮：已有记录时变为橙色「Update Test Record」，新记录保持黑色「Save Test Record」
- 顶部状态徽章：「Updating existing record」/ 「New record」状态指示
- My Progress 里同一套题只有一条记录，不重复

## 计时器功能（第五轮）

- 新增 `src/components/TestTimer.jsx`：倒计时组件
- 预设5种时长：1/10/15/20/30 分钟
- SVG 圆环进度条，颜色随时间渐变（绿→黄→橙→红）
- Start/Pause + Reset 控制
- 倒计时结束时 Web Audio API 发出 beep 提示音
- 每个预设附带使用场景说明（阅读/听力专项练习等）
